# postgres-template.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-db
  namespace: trackpak
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-db
  template:
    metadata:
      labels:
        app: order-db
    spec:
      containers:
        - name: postgres
          image: postgres:15
          env:
            - name: POSTGRES_USER
              value: developer
            - name: POSTGRES_PASSWORD
              value: trackPak_dev_password
            - name: POSTGRES_DB
              value: trackpak-orderdb
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: order-db-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: order-db-data
          persistentVolumeClaim:
            claimName: order-db-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: order-db-pvc
  namespace: trackpak
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: order-db
  namespace: trackpak
spec:
  selector:
    app: order-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
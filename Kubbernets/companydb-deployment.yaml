# postgres-template.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: company-db
  namespace: trackpak
spec:
  replicas: 1
  selector:
    matchLabels:
      app: company-db
  template:
    metadata:
      labels:
        app: company-db
    spec:
      containers:
        - name: postgres
          image: postgres:15
          env:
            - name: POSTGRES_USER
              value: developer
            - name: POSTGRES_PASSWORD
              value: trackPak_dev_password
            - name: POSTGRES_DB
              value: trackpak-companydb
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: company-db-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: company-db-data
          persistentVolumeClaim:
            claimName: company-db-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: company-db-pvc
  namespace: trackpak
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: company-db
  namespace: trackpak
spec:
  selector:
    app: company-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
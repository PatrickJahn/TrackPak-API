# location-service-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: location-service
  namespace: trackpak
spec:
  replicas: 2
  selector:
    matchLabels:
      app: location-service
  template:
    metadata:
      labels:
        app: location-service
    spec:
      containers:
        - name: location-service
          image: trackpak-locationservice
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: ConnectionStrings__Postgres
              value: Host=location-db;Port=5432;Database=trackpak-locationdb;Username=developer;Password=trackPak_dev_password
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: location-service
  namespace: trackpak
spec:
  selector:
    app: location-service
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
# postgres-template.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: location-db
  namespace: trackpak
spec:
  replicas: 1
  selector:
    matchLabels:
      app: location-db
  template:
    metadata:
      labels:
        app: location-db
    spec:
      containers:
        - name: postgres
          image: postgres:15
          env:
            - name: POSTGRES_USER
              value: developer
            - name: POSTGRES_PASSWORD
              value: trackPak_dev_password
            - name: POSTGRES_DB
              value: trackpak-locationdb
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: location-db-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: location-db-data
          persistentVolumeClaim:
            claimName: location-db-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: location-db-pvc
  namespace: trackpak
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: location-db
  namespace: trackpak
spec:
  selector:
    app: location-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
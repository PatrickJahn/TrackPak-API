# route-service-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: route-service
  namespace: trackpak
spec:
  replicas: 2
  selector:
    matchLabels:
      app: route-service
  template:
    metadata:
      labels:
        app: route-service
    spec:
      containers:
        - name: route-service
          image: trackpak-routeservice
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: ConnectionStrings__Postgres
              value: Host=route-db;Port=5432;Database=trackpak-routedb;Username=developer;Password=trackPak_dev_password
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: route-service
  namespace: trackpak
spec:
  selector:
    app: route-service
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
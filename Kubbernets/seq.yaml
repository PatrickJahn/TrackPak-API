apiVersion: apps/v1
kind: Deployment
metadata:
  name: seq
  namespace: trackpak
spec:
  replicas: 1
  selector:
    matchLabels:
      app: seq
  template:
    metadata:
      labels:
        app: seq
    spec:
      containers:
        - name: seq
          image: datalust/seq:latest
          env:
            - name: ACCEPT_EULA
              value: "Y"
          ports:
            - containerPort: 5341
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: seq
  namespace: trackpak
spec:
  selector:
    app: seq
  ports:
    - port: 5341
      targetPort: 5341
    - port: 5342
      targetPort: 80
  type: ClusterIP
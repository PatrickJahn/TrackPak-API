# employee-service-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: employee-service
  namespace: trackpak
spec:
  replicas: 2
  selector:
    matchLabels:
      app: employee-service
  template:
    metadata:
      labels:
        app: employee-service
    spec:
      containers:
        - name: employee-service
          image: trackpak-employeeservice
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: ConnectionStrings__Postgres
              value: Host=employee-db;Port=5432;Database=trackpak-employeedb;Username=developer;Password=trackPak_dev_password
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: employee-service
  namespace: trackpak
spec:
  selector:
    app: employee-service
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP